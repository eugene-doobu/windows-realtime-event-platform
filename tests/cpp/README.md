# C++ Test Placeholder

The repository now has a dedicated `tests/cpp/` directory, but no native test
harness is wired yet.

That is intentional for the first skeleton pass. The next C++ milestone should
add a lightweight native test target once the kernel starts mutating state
instead of returning a stub result.

